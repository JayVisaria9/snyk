<?php

/**
 * @file
 * Bug.
 */

$existing_data = "data";
if ($existing_data == $existing_data) {
  echo "No Error";
}
$existing_data = "data";
if ($existing_data == $existing_data) {
  echo "No Error";
}
